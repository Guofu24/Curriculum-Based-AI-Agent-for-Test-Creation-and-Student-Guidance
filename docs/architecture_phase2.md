# Archived Phase 2 Architecture

This file is kept only for historical context.

The active runtime story has moved to:

- `README.md`
- `backend/README.md`
- `docs/phase3_eval_workflow.md`
- `docs/error_analysis.md`
- `docs/ui_flow.md`

What remains valid from Phase 2:

- strict scope is still hard-enforced
- retrieval is still section-aware
- legacy runtime stays unmounted
- document-first naming remains the active surface

What changed in Phase 3:

- eval data is now split into `dev` and `held_out`
- error analysis is an explicit workflow
- feedback events carry richer review metadata
- dashboard and review UI emphasize quality monitoring
- old Phase 2 eval assets are archived instead of active
